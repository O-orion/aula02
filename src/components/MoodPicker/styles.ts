import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: 20
    },
    title: {
        fontSize: 24,
        color: '#333',
        marginBottom: 20,
    },
    moodList: {
        flexDirection: 'row',
        gap: 20
    },
    moodButton: {
        alignItems: 'center'
    },
    emoji: {
        fontSize: 40
    },
    label: {
        fontSize: 16
    }
})
